[project]
name = "codex-minimal-outrigger"
version = "0.1.0"
description = "Thin workflow wrapper around Codex CLI."
requires-python = ">=3.12.3"
dependencies = [
    "typer>=0.12.0",
]

[project.scripts]
cmot = "main:app"
